#include <stdio.h>
int main() {
    int age;
    scanf("%d",&age);
    if((age>=18)&&(age<=70)){
        printf("eligible for licence");
    }else{
        printf("not eligible for licence");
    }
    return 0;
}
