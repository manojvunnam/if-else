#include <stdio.h>
int main() {
    int attendence;
    scanf("%d",&attendence);
    if(attendence>=75){
        printf("eligible for exam");
    }else{
        printf("not eligible for exam");
    }
    return 0;
}
